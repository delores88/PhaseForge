# Tested with standalone CPython 3.12.14, Windows x64. Use an isolated venv.
cadquery==2.6.1
jsonschema==4.25.1
cadquery-ocp==7.8.1.1.post1
vtk==9.3.1
# CasADi 3.8.0 and NLopt 2.11.0 Windows wheels share a SWIG runtime table
# across incompatible native allocators, crashing at interpreter shutdown.
# 3.6.7 retains a distinct SWIG runtime and passes native exit/geometry tests.
casadi==3.6.7
nlopt==2.11.0
