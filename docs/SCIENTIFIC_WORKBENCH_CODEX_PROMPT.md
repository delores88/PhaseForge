# Codex implementation prompt: make PhaseForge a working scientific laboratory

Implement this request in `<repository-root>`. This is an implementation assignment: build, test, install the updated Windows application on this machine, then pursue acceptance of that Windows release in the DeloresAI marketplace. The research and simulation capabilities below are the main deliverable. Marketplace polish is a small final workstream. Do not stop after a plan, prettier scenes, another capability registry, or a collection of adapters that have never executed an experiment.

## Product outcome and priorities

PhaseForge should let a researcher describe a scientific or mathematical question and work with an AI team that can obtain relevant data, choose defensible models, prepare executable experiments, run real scientific engines, inspect numerical results and rendered observations, test hypotheses, and iterate. It should serve as a collection of simulation laboratories and training environments, with extensible instruments for physics, biology, chemistry, mathematics, and other scientific pursuits. Researchers should not need to know a solver's command line or how to program a 3D engine.

The long-term aim is to replace selected expensive physical experiments or parts of wet-lab workflows where predictive validity can actually be demonstrated. Deliver useful, calibrated endpoints and evidence of their limits; do not claim that a visually convincing scene, a completed calculation, or a model's agreement proves biological efficacy. Broad scope requires extensible execution and honest capability boundaries, not a claim that one small equation engine can simulate all science. When a requested system is beyond present data, scale, or validation, explain the gap and offer a scientifically relevant bounded study. Never silently replace the requested mechanism with an unrelated toy that happens to display the same nouns.

I love the existing detailed Blender stills. Preserve that quality and bring simulation playback toward it. Scientific illustration is also a valuable use case: a request for a static scientific 3D illustration should use Blender without requiring me to name the engine. Choose the deliverable from the current request and relevant conversation, never from a stale view or mode. A request to investigate a phenomenon should initiate an experiment workflow appropriate to the question. Keep normal scientific questions and explanations comfortable in chat, too.

My clarified CAR-T request is for a realistic scientific 3D illustration with useful labels. Mentioning medical communications or an audience does not request marketing headlines, banners or step-card layouts. Preserve any earlier communications plate, but do not count it as fulfilling this clarified request. Use verified retained structures where available, preserve an interacting complex's common coordinate frame, and identify conceptual cell geometry, unresolved domains and inset magnification. Photorealism is not atomic accuracy. Ask one concise question only when the requested deliverable or scientific scale is still unclear; use prior answers instead of asking again.

Prioritize in this order: real execution and scientific validity; durable agent work and memory; measured simulation visualization and AI observation; trustworthy chat and job controls; Windows installation and marketplace delivery. Build these as integrated, demonstrable workflows rather than disconnected subsystem checklists.

Deliver this through the mandatory checkpoints below. Choose a small concrete replacement boundary and preserve working pieces. Do not spend the assignment building a universal abstraction without proving that it helps a researcher run and interpret an experiment.

**Mandatory delivery sequence**

1. **Prove one complete lab in an installed Windows build before expanding the engine catalog.** Choose a tractable scientific question with documented inputs and an independent reference. Prefer a molecular/biophysical study that establishes the missing real-engine integration when feasible. A researcher must be able to ask the question in ordinary chat, have the app prepare and execute the experiment, inspect a detailed animation driven by its numerical output, and receive an explanation grounded in actual images and measurements. Demonstrate a controlled parameter change with a defensible measurable effect, navigation during work, and recovery after interruption. Implement the necessary parts of orchestration, visualization, and chat together to achieve this; installing an engine or displaying a trajectory alone does not pass.
2. **Prove that the first lab generalizes.** After the first workflow works, have a reviewer specify an additional scientifically valid variant within the declared supported scope: a changed initial condition, parameter regime, boundary condition, or observable that was not a hardcoded showcase case. Run it through the normal chat and tool path without developer edits to the application or manual preparation of its answer. Agent-generated experiment code and inputs inside the supported managed environment are allowed and must be retained. Check the new outcome against a reference or independently calculated expectation. A prerecorded replay, prewritten answer, or forced choice among demonstration buttons does not pass.
3. **Prove extension into a second distinct scientific domain through the same orchestration and evidence infrastructure.** Reuse the working contracts, add the needed scientific adapter/instruments, and repeat the relevant numerical, visual, and ordinary-chat checks. This establishes that the platform is extensible rather than one bespoke demonstration.
4. **Complete the remaining scope and release checks.** Continue through the other required studies, ML evaluation, recovery/compaction tests, interface fixes, final local installation, and Windows marketplace work. Passing the first lab is an early milestone, not permission to call the full assignment complete. Keep an explicit record of every unfinished criterion.

These checkpoints govern sequencing and evidence, not repeated user-approval requests. Present concise evidence at each checkpoint and continue autonomously when it passes. If a checkpoint fails, fix the underlying behavior and rerun the affected checks before claiming it passed. If an external blocker prevents completion, identify it precisely and continue independent authorized work; do not quietly remove the criterion or substitute a decorative result.

The new variant must execute fresh computation for a scientifically meaningful intervention or measurement, with a distinguishable outcome; changing names, colors, or playback settings does not qualify. Do not add a special UI path or manually repair the database to pass it. If it exposes a general application defect, fix that defect and test another fresh variant afterward. For interruption recovery, specify the in-flight stage and reconcile work whose completion is uncertain; a documented restart from valid inputs or the last supported checkpoint is acceptable when the solver cannot resume mid-step. The second domain may use different solvers and data formats, but must reuse the orchestration and evidence infrastructure; another preset of the first lab's equations does not establish cross-domain support.

**Completion must be established outside the implementing agent's narrative**

Before executing each acceptance study, record its supported scientific scope, reference source, expected observables, numerical tolerances, and failure conditions. Use executable numerical checks against analytic results, an independent implementation, or held-out experimental measurements as appropriate. A second agent may review the implementation and evidence, but another LLM agreeing is not independent scientific validation. Do not derive the expected answer from the same application output being tested or loosen a tolerance after failure merely to obtain a pass. Any justified change to the model or acceptance method must preserve the failed result and explain the scientific reason for the revision.

Retain a compact acceptance ledger linking each requirement to the exact installed build, inputs, run/tool records, checker results, and reproducible artifacts. Record separate outcomes for execution correctness, numerical/reference agreement, visual fidelity to the data, and predictive validity for the claimed real-world endpoint. Mark untested claims untested. Agreement with a mathematical reference can validate a numerical calculation without establishing biological realism. Claims about replacing a particular physical or wet-lab measurement require comparison with appropriate empirical evidence and, where needed, domain-specialist review.

Show the user the actual installed workflow and provide the artifacts needed for inspection and rerunning it. Neither screenshots alone, an engine's name, passing self-authored superficial tests, nor a final report saying the architecture is complete establishes delivery. Judge success by observed behavior, reproducible calculations, and the ability to handle the new variant above.

## Start from the actual product and evidence

Read `docs/OBSERVATION_NOTES_2026-09-11.md`, relevant repository instructions, and the installed app's saved workspaces. Preserve existing user data and useful working components. Identify the current repository commit, installed build, running backend, and database; do not confuse a newer checkout with the installed binary. Back up the existing local database and artifacts before migrations or installation, and prove migration and rollback behavior. The observed data root was `%LOCALAPPDATA%/PhaseForge/PhaseForge/data/`.

The September 11 observation found 8 projects, 67 messages, 12 numerical runs, 45 provider-call records, one durable research task, and five Blender jobs. Recheck current counts rather than treating these as permanent constants. The important evidence is:

- All retained numerical runs used the state-vector ODE path. The viral-envelope demonstration solved synthetic helical approach trajectories in about 0.174 seconds; it did not model molecular binding, Brownian motion, reactions, or membrane mechanics. Later imported molecular structures were rendering inputs, not inputs to that earlier calculation.
- The detailed Blender job rendered a still and exported with animations disabled. It lacked a simulation run or trajectory input. This explains the disconnect between the attractive white structure and the much simpler replay.
- The interactive viewer uses Three.js/WebGL. `frontend/src/lib/scene-viewer.mjs` around line 108 maps the entire retained interval to roughly 15 seconds at 1x playback, regardless of scientific duration. Separately, `lab.js` limits displayed frame/entity records, while the backend defaults to 1,200 saved frames and permits at most 20,000 in this schema. These are separate presentation/retention constraints, not proof that the solver calculated only a few seconds. Basic adaptive display resolution already exists; build on measured behavior rather than describing all hardware adaptation as absent.
- The backend reported GROMACS, OpenMM, CP2K, xTB, LAMMPS, and Open Babel unavailable in its environment. Engine discovery names alone do not provide runnable integrations.
- The black-hole project did not acquire a usable stellar catalog. Some retained searches returned unrelated Sagittarius entries. A source citation or JPEG is not simulation-ready observational data.
- Several long waits were output-budget failures: seven calls exhausted the 12,000-token output cap. Four electron-project failures had approximately 8,000 input tokens. Distinguish output exhaustion from input-context overflow, invalid structured output, tool failure, and lost task continuity.
- Memory assembly keeps only 24 chat messages and clips selected artifacts to 1,800 bytes. A retained specialist contribution ends mid-sentence. Storing full artifacts is insufficient unless agents can retrieve them.
- Asking whether an HIV run was useful was routed into experiment mode with auto-run enabled, producing another run. An explanation must not silently become an experiment.
- Markdown and artifacts render inconsistently; scientific notation and tables are poorly supported. Raw viewport metadata appears in messages. Chat expansion is defeated by responsive CSS. An old run can be displayed beside a newer experiment title and later chat interpretation.
- The saved research task showed one specialist and one cycle. Agent cards exposed little useful work, and a completed task had a partly filled elapsed-budget bar. The existing AI viewport context contains scene metadata, not rendered frame images.

The additional read-only model/navigation audit established specific paths, not a blanket claim that Settings overrides all chat:

- Ordinary chat sends the selected provider, model, and reasoning effort and the backend honors a nonempty request model. Repairs reuse that client. Research tasks snapshot these choices correctly.
- `ResearchWorkbench.jsx` around line 505 sends `provider:null` and no model or reasoning choice to Explain Findings. `backend/src/agent/mod.rs` around line 261 uses the configured provider default and `status.model`. Explicit and automatic explanations therefore bypass the chat picker.
- Chat selection is persisted separately from backend Settings defaults. Switching provider tabs resets selection to that provider's Settings model. The picker can display a recommended reasoning effort while sending a null effort.
- `ResearchWorkbench.jsx` around line 310 silently blocks project switching while `sendingRef` or `busy` is set; its project dropdown is disabled. Sidebar project buttons still look clickable. Workspace view buttons remain enabled, but chat/run/explanation completion handlers force World or Findings and can pull the user away from the view they chose.
- Busy state normally spans a model request, but only submission for an ordinary numerical run. Do not inaccurately assume every solver blocks every view for its entire duration. Pending chat UI state belongs to the mounted workbench and is not clearly reconstructed after leaving it. Verify route changes, refresh, disconnect, and restart with delayed providers and actual workers before asserting precise failure behavior.

These audit paths were compared with installed source commit `7471ff72` and matched at inspection time. Reproduce against your starting version, then add regression coverage for the confirmed defects.

## 1. Build the actual scientific execution layer

Keep and improve the existing numerical engine where it is appropriate, but remove its role as the universal ceiling. Introduce an extensible experiment and instrument contract with versioned adapters for external solvers, numerical libraries, ML runtimes, and project-scoped executable experiment code. Research current primary documentation and licensing before choosing dependencies. Favor established engines that can genuinely run on this Windows machine and use its RTX 4090 Laptop GPU where the algorithm benefits. Do not select a stack merely because its name appears in the existing registry.

Each adapter must perform environment discovery, installation or provisioning when necessary, an actual readiness smoke test, input preparation and validation, asynchronous execution, progress/log reporting, cancellation, output parsing, and result registration. Support checkpoint/resume where the engine supports it, and explicitly report restart-from-input where it does not. A successful version probe is not a passed scientific capability test.

The experiment record must bind the question and hypothesis to exact data, model equations or engine inputs, units, coordinates, initial/boundary conditions, parameters, stochastic seeds, solver settings, engine/environment versions, input hashes, and declared observables. Preserve trajectories, fields, meshes, checkpoints, raw outputs, analysis, source citations, failures, and lineage under immutable run identities. Support arrays and external artifacts rather than forcing every scientific object into one huge JSON response or short list of scalar states.

Build usable tools that agents can call to search and retrieve scientific sources, import and validate datasets/structures, inspect the available hardware and engines, prepare inputs, write/run experiment code within its environment, launch and monitor a solver, inspect results, compute metrics, request visual observations, and run further tests. Validate tool arguments and outputs. Return durable job IDs for expensive operations; agents should not need to hold an HTTP request open throughout a simulation. Tool errors should be actionable and retained with the job, including stdout/stderr references without exposing credentials.

Treat data acquisition as part of execution. Check that a retrieved record actually concerns the requested system and that the required numerical dataset was acquired with units, provenance, licensing, and parsing checks. Clearly identify synthetic inputs. Missing observations must not become invented measurements. Downloading a scientific image must not count as acquiring the underlying experimental data.

Build a local environment manager that can provision missing scientific dependencies and expose them through these adapters. Use reproducible, versioned, project-scoped environments and cached downloads from trusted upstream sources. Generated experiment code must have an actual execution boundary with restricted filesystem access, resource limits, network policy, and controllable child processes. A Python virtual environment isolates packages, not untrusted code; do not advertise it as a security sandbox. Select and demonstrate a suitable Windows isolation mechanism, and disclose any prerequisite it needs rather than silently executing unrestricted host commands. Keep provider secrets out of experiment environments, logs, images, and export bundles. Do not require researchers to repeatedly assemble the toolchain manually.

Resource management must reflect real available RAM, VRAM, CPUs, and supported acceleration. Estimate expensive jobs before launch, select feasible resolution/batch sizes, and monitor them. Handle out-of-memory, solver divergence, missing dependencies, and failed inputs with bounded diagnosis and recovery. Record any changed fidelity, timestep, precision, batch size, or model assumption; never silently weaken the experiment to make it pass. Distinguish renderer GPU use, ML acceleration, and solver acceleration in the UI and evidence.

Benchmark the actual machine and provide automatic graphics-quality selection with user overrides. Adapt display resolution, geometry detail, effects, and render sampling independently of the scientific model and numerical accuracy. Reserve resources for the desktop and active solvers, and reassess contention during work. Allow a responsive preview while higher-quality frames render asynchronously. The LLM should configure reusable engines and generate necessary experiment code; it must not generate every timestep or animation frame as text. Report whether a bottleneck is model/tool preparation, scientific computation, rendering, memory, or storage rather than attributing all delay to the AI or GPU.

Provide virtual instruments alongside solvers: spatial/time sampling, detectors, concentration or population measurements, spectra where supported, image acquisition, uncertainty estimates, comparisons, and domain-appropriate statistics. Instruments must calculate from recorded scientific state and declared observation models. Their outputs should be reusable inputs to later experiments and exportable for independent analysis.

## 2. Make scientific validation and ML part of the loop

Agents should propose a falsifiable question, choose an appropriate model, execute it, compare controls and alternative hypotheses, evaluate uncertainty and numerical validity, and select a useful next experiment based on evidence. A reviewer must be able to reject a hypothesis or challenge the experiment design. Prevent circular validation in which the same agent invents easy checks and then treats passing them as scientific confirmation.

Distinguish operational completion, numerical stability, validation against a reference, and support for a hypothesis. Use dimensional checks, conservation laws where applicable, convergence studies, sensitivity analyses, stochastic replicates, analytic solutions, independent implementations, and held-out empirical data as appropriate. Define tolerances before inspecting outcomes and retain failures. A known special solution of the three-body problem must not be presented as solving the general problem.

Support ML for useful tasks such as calibrated surrogate models, parameter estimation, experiment selection, and image/field analysis. Keep training, validation, and test data separate; record seeds, data/model versions, baselines, evaluation metrics, uncertainty, and applicable ranges. Verify surrogate predictions against the original solver on held-out cases and test out-of-domain behavior. A fast ML prediction needs a visible distinction from a full solver run. The LLM must not provide fabricated training labels or act as the sole judge of scientific accuracy.

Split evaluations by experimental conditions where appropriate, not only by adjacent frames from the same trajectory. Report total compute cost including training and demonstrate a useful accuracy/speed tradeoff for a stated task. Require uncertainty or direct-solver fallback outside the validated range.

Complete integrated examples across several domains rather than claiming universality from one attractive demo. At minimum deliver:

1. A classical mechanics/mathematics experiment with multiple interacting bodies, meaningful invariants, refinement tests, and a controlled perturbation that changes a measurable outcome.
2. A spatial field experiment, such as diffusion/reaction or a wave equation, with actual evolving fields, declared boundary conditions, an analytic or independent benchmark, and instruments that measure the field.
3. A scientifically bounded molecular/biophysical experiment using a real engine, documented structures/parameters, physical observables, controls, and a defensible reference. Use a tractable, well-understood system to establish the adapter and measurement pipeline. Preserve the viral illustration as illustration; do not turn an orbiting scaffold into an efficacy claim or pretend atomistic whole-organism behavior is feasible on a laptop.
4. An ML-assisted study using real outputs from one of these engines, with a held-out comparison against solver results and documented limitations.

These are release demonstrations and regression cases, not hardcoded domain-specific UI demos. At least one new variant must be constructed through normal user chat using the same tool interfaces an ordinary researcher receives. For requests beyond the installed set, the agent must be able to build a compatible experiment in its managed environment or identify the precise scientific or execution prerequisite, rather than returning a generic capability-gap message forever.

Before implementing each demonstration, name its concrete problem, chosen engine/version, input assets, expected measurable phenomenon, reference, and acceptance tolerance. Retain an export another reviewer can rerun independently from the saved inputs. An engine name, successful import, or attractive result card does not satisfy these criteria.

## 3. Build durable, long-running agent orchestration

Make the backend own research sessions and jobs independently of any visible component or request connection. Implement a real repeated tool loop: plan a bounded next action, execute it, inspect the result, update shared research state, and continue while useful work and authorization remain. Specialist agents should have concrete tasks, outputs, and coordination through durable artifacts; multiple role labels and one model response are not a research team.

Persist session/job/agent identities, statuses, parent-child relationships, run ownership, inputs, outputs, tool-call receipts, model choices, event sequence positions, checkpoints, and terminal reasons. Use idempotency and recovery rules so reconnecting or resuming cannot duplicate paid calls or submit an experiment twice. Handle provider disconnects, rate limits, partial responses, validation errors, worker crashes, and app restart separately. Recover surviving remote/local jobs where possible and clearly mark interrupted jobs that require restart. Define window-close/tray and full-Quit behavior; do not imply work continues while the machine is off.

Put duration controls directly in chat. Let the user choose a practical preset or custom time limit, or explicitly turn the timer off. Snapshot an actual deadline into the orchestrator and propagate remaining time and cancellation to all agents, model requests, simulations, and subprocesses. Make elapsed versus remaining time clear. At expiry, stop scheduling work, cancel outstanding calls where supported, stop child processes with a bounded graceful shutdown and enforced termination if needed, checkpoint results, and report completed and unfinished work. Cover sleep/restart and timer changes without resetting the budget accidentally. Turning the timer off removes the wall-clock limit, not Stop, resource controls, or separately configured spending limits. Do not silently impose another short global time ceiling.

Let users pause where supported, stop, resume, and steer an active session. A new clarification or scientific question should not destroy its research objective. Capture changed constraints explicitly, apply them to future actions, and preserve results produced under earlier assumptions. Use useful progress milestones, not a fake completion percentage derived from elapsed budget.

Treat the selected duration as a maximum, not a target to fill with repeated calls. Stop early when the objective is achieved, evidence warrants stopping, or a real blocker prevents progress. Persist the reason and remaining work so a later continuation can resume deliberately.

Treat context management as an explicit subsystem. Preserve original conversation and artifacts durably, maintain a structured research ledger of objectives, constraints, decisions, evidence, rejected approaches, unresolved issues, and next actions, and retrieve relevant source material by stable IDs. Use token-aware compaction before exhaustion and provider-supported continuation/compaction where available. Keep large arrays, logs, code, and images as retrievable artifacts with concise summaries. Do not clip arbitrary byte slices mid-sentence and call that memory. Preserve tool-call/result associations and important references across compaction; keep provider-specific opaque state opaque. A resumed agent must be able to reopen the full evidence.

Manage input budgets separately from reasoning/visible output budgets and whole-session duration. Split oversized tasks into useful tool steps and artifacts; do not repeatedly retry the same oversized monolithic proposal or simply raise every cap. Explain the actual reason for a failure. Preserve metering and user limits across retries, compaction, and resume, and avoid duplicate billing through duplicate work.

Use the current official API capabilities appropriate to each configured provider. OpenAI explicitly supports application-executed function tools, asynchronous background responses, and compaction; these do not automatically implement PhaseForge's local solver supervisor. Verify supported capabilities per model and account rather than assuming the Codex application's tools are automatically inherited by an API client. References: [function calling](https://developers.openai.com/api/docs/guides/function-calling), [background mode](https://developers.openai.com/api/docs/guides/background), and [compaction](https://developers.openai.com/api/docs/guides/compaction).

## 4. Connect measured simulations to detailed rendering and visual observation

Create a consistent path from solver state to an interactive scientific scene and optional high-quality rendered animation. Bind geometry, transforms, fields, colors, and time to immutable run data with stable entity IDs, units, and coordinate transforms. Preserve detailed geometry/materials and use instancing, level of detail, and sensible streaming/caching to fit this machine. Camera changes should not rerun physics. Rendering resolution, geometry detail, recorded sampling, integration timestep, and scientific model fidelity are different controls; communicate them accurately.

Bring playback closer to the quality of the existing Blender stills: purposeful lighting and materials, readable structures and spatial fields, inspection tools, and useful scale. Use real-time rendering for responsive inspection and a trajectory-driven Blender animation/export path for higher-quality outputs where appropriate. Keep animation linked to the exact run and frame times. Any interpolation, exaggeration, illustrative geometry, or coarse-graining must be identifiable. Do not make movie-quality real-time path tracing a hidden prerequisite for interactive science.

Make an explicit renderer decision during the first lab. Inspect and benchmark the existing Three.js viewer on representative molecular geometry and evolving field data. Evaluate an alternative when a required capability or measured performance warrants it. Retain, extend, or replace the viewer based on demonstrated visual quality, data fidelity, interaction latency, memory use, and Windows packaging, and document that decision. A different engine name alone is not an improvement; keeping the existing implementation unchanged without meeting the visual acceptance criteria is also insufficient. Preserve Blender for requested illustrations and high-quality trajectory-driven animation.

Separate four time controls: the scientific time horizon with physical/scaled units, solver integration and output-sampling intervals, the user's wall-clock research budget, and playback/export duration and speed. Let users request a longer scientifically meaningful horizon and continue compatible runs from supported checkpoints. Retain and stream trajectories in chunks with disk-backed storage, bounded caches, seeking, and explicit retention controls so longer studies are not silently reduced to a tiny fixed frame set or short clip. Preserve sampling sufficient for declared observables; visual downsampling must not discard the authoritative data needed for those measurements without an explicit retention decision. Do not lengthen a result by looping, interpolating, or slowing a short trajectory and calling that additional simulated science. Explain when the requested physical horizon is infeasible at the chosen model fidelity and offer a quantified alternative.

Replace the unconditional 15-second replay mapping with explicit, understandable timeline and playback-duration controls. Let a researcher inspect a long study at a useful pace, seek to events, choose a time window, and export a longer animation when requested. Show what one second of playback represents in simulated units. Verify both an extended numerical horizon with newly calculated states and independently adjustable presentation duration.

Provide an explicit Export video action with a local Save/download result. Let users select resolution (including 720p, 1080p, and 4K where supported), playback speed or target video duration, simulation time range, frame rate, camera/view, and optional scientific labels/legend. Show the resulting video duration and an estimated render time before starting, with estimates identified as estimates. Export a broadly compatible MP4 by default using a verified available encoder; disclose additional dependencies if needed. Render from the selected saved numerical trajectory and scene, rather than recording the desktop UI. Slower playback changes presentation, not the calculated physical trajectory; any interpolated frames must respect the recorded data and remain distinguishable from newly computed states in provenance.

Treat video rendering as a durable background job with progress, cancellation, completion notification, and a working save/open-file action. Users must be able to navigate elsewhere during export. Export quality must be independent of the adaptive preview resolution; do not silently reduce a requested export resolution or speed under load. Estimate feasibility, queue work, or explain a resource limit. Retain export settings and source run/time-range identity alongside the video, and avoid presenting a cancelled or incomplete file as a completed download.

The user and AI should be able to orbit, pan, zoom, choose a camera, inspect entities, select time ranges, compare runs, and examine slices or fields where relevant. Add actual image observation tools: render/capture selected frames or scientifically useful camera views and send those images to a vision-capable model with run ID, timestamps, camera, scales, and relevant measurements. Merely serializing camera metadata is insufficient. Retain the observed frame references and the resulting analysis.

Make camera interaction substantially easier in both the simulation viewer and the editable 3D illustration/scene viewer. Provide smooth, predictable mouse and trackpad orbit, pan, zoom, and tilt/elevation controls, plus camera roll where useful, accessible on-screen controls, keyboard alternatives, and precise camera settings. Include an orientation gizmo, standard views, fit-all, focus/fit-selection, reset/home, and saved camera views. Derive zoom limits, movement sensitivity, and clipping planes from scene scale so molecular close-ups and astronomical scenes remain navigable without disappearing or jumping. Keep camera state stable during playback, visual edits, and navigation back to a view. Prevent scene gestures from accidentally scrolling chat or the page and provide clear control hints.

Support natural-language visual edits through tools that target the selected saved run/scene and stable entity IDs: for example, "make this blue," "increase contrast," "highlight that protein," "dim everything else," "zoom into the selected region," or "tilt the view so I can see underneath." Expose equivalent direct controls for colors, materials, contrast/exposure, backgrounds, outlines/highlights, opacity, visibility/isolation, labels, and camera. Update the interactive view promptly, support undo/redo, and persist presentation revisions separately from immutable solver inputs/results. Maintain legends when editing data color scales. Resolve ambiguous target references from selection and scene context, with clarification only when necessary.

Visual-only edits must not submit a new solver job or change numerical measurements. Reuse the retained trajectory, geometry, and assets; invalidate only the necessary display/render outputs. A saved flat image supports image zoom and pan; changing its 3D viewpoint or scene lighting requires rendering again from the retained editable scene, which is separate from rerunning the scientific experiment. Make any needed high-quality rerender a visible cancellable background job. A change to physical parameters, interactions, or boundary conditions belongs to an explicit experiment revision, not an unnoticed visual edit.

Make visual monitoring an operational part of simulation research sessions. Schedule observations at meaningful solver checkpoints, detected events, and final review, within the user's time and usage budgets; expose its enabled state and sampling policy in chat. Monitor intermediate output while a run continues when the engine supports it. For engines that only provide final output, say so and inspect the retained trajectory afterward. Use numerical/event detectors to select informative frames or frame sequences, rather than sending every display frame to an LLM. The observer must be able to request another camera, slice, time range, or quantitative instrument reading, then pass an evidence-linked finding to the orchestrator for a follow-up test within the authorized study. Keep monitoring active when the user navigates elsewhere. Record what was actually inspected and when, and never imply uninterrupted observation of unsampled moments.

Use visual analysis to detect or suggest patterns, anomalies, segmentation/counts, qualitative behavior, and new tests. Separate observed image features from inferred mechanisms and compare visual claims to numerical instruments. Test sensitivity to camera, lighting, occlusion, and rendering choices. AI must be able to request another view or measurement and express uncertainty; attractive imagery must not become an independent validation oracle.

Preserve a separate, intentional scientific illustration workflow for explicit static-image requests, including detailed Blender output and editable assets. Label scientific provenance and illustration scope without covering the product in repeated disclaimers. Do not start a solver for a simple illustration request or produce a static scene as the completion of a simulation request.

A request such as "make a scientific illustration of this" is sufficient to enter the illustration workflow; the user does not need to name Blender or request an experiment first. Honor requested colors, palettes, materials, lighting, labels, and backgrounds in both illustrations and simulation views. Support colored scientific structures and fields rather than a fixed monochrome style. Preserve meaningful legends for colors encoding measured quantities, and keep purely visual style changes separate from the underlying scientific calculation. Retain the user's visual preferences across follow-ups and conversation compaction.

## 5. Make chat and background work trustworthy and usable

Allow navigation between every project, view, and main page while model, solver, render, and analysis jobs run. Scrolling or switching projects must not cancel work, lose pending messages, hide Stop permanently, or move results to the wrong project. Async completion must not force navigation or steal focus. Restore authoritative status on return, reload, and reconnect.

Show a small spinning circle on the relevant sidebar project/job while it is active. On completion, replace it with a blue unread dot that clears when that completed job/result is opened. Persist read/seen state and handle multiple jobs completing in one project; viewing one must not erase unseen completions from others. Distinguish errors, cancellation, paused work, and completion accessibly without relying only on color. Provide a compact global jobs surface with project ownership, current action, elapsed time, controls, and a direct path to results.

Expose useful agent activity in chat: current objective, specialist assignments, tool actions, progress, intermediate findings, revisions, and final outcomes, with expandable evidence and timestamps. Stream available text and durable work events. Show concise work/reasoning summaries where supported, not private chain-of-thought or invented thinking. Keep implementation metadata out of conversational prose; detailed logs remain inspectable.

Make the logical workflow and agent handoffs visible in a persistent activity timeline: which agent is working, its assigned question, why a next step was selected, which tool or solver it called, what evidence came back, and which agent received the result next. Link child tasks and handoffs to their inputs and outputs, and distinguish waiting, executing, reviewing, retrying, and finished states. These must reflect actual recorded activity, not canned thinking messages. After compaction or reconnection, preserve this timeline and show a concise continuation summary of the objective, key decisions, and next action so the user can follow the work.

Use a mature, safely rendered Markdown implementation consistently across chat, reports, and artifacts: links/citations, ordered and nested lists, tables, fenced code with language support, inline code, and mathematical notation. Fix the tiny raw-artifact boxes, chat-width CSS conflict, contrast, whitespace, long values, and responsive overflow. Round displayed measurements sensibly while preserving full precision in exports. Lead scientific explanations with the result and meaning, with linked evidence and expandable detail.

Unify intent handling for scientific discussion, explanation, experiment execution, and explicit illustration. Remove accidental execution caused by sticky Build & run flags. A request to explain an existing run must remain an explanation. Do not require a permission dialog for each authorized tool action; make the user's session intent and budgets durable. Keep active experiment, selected historical run, evidence report, viewport title, and chat references consistent, with clear run-linked navigation.

Make chat the single model-selection surface. Settings should configure provider credentials/endpoints and appropriate usage/resource preferences, not a competing active model. Discover models available to the chosen provider/account, filter for the needed capabilities, and show the best six eligible choices ordered from least to most capable within that shortlist. Use maintained capability metadata and measured/documented suitability, not alphabetic IDs or creation timestamps as a proxy for intelligence. If fewer than six eligible models are accessible, show the real available number. Never invent models or substitute six reasoning levels for six models.

Replace the existing selector with the visual style in the attached reference: a substantial rounded track, six clear positions, a prominent thumb, and a readable model/effort label above. Add a tasteful gradient, rather than the flat blue fill. Keep it compact enough for chat and usable with mouse, keyboard, touch, high contrast, and reduced motion. The reference is `<Codex user-data directory>/codex-remote-attachments/01a08e6d-ce57-7410-8152-91c66eb818e7/3363BA29-E389-4C9E-9C31-59A39D6FD199/1-Photo-1.jpg`.

Persist explicit per-conversation choices and snapshot provider/model/effort into every launched request and job. The displayed effort must match the outgoing request. Apply the choice consistently to ordinary chat, repairs, explicit/automatic explanations, research specialists, builder, reviewer, illustration calls where compatible, and resume. Document any distinct image-generation model when needed. Changing the picker should affect future work, not silently mutate an in-flight task. If a model becomes unavailable, show and record the fallback decision; do not silently use a Settings default. Use the user's configured OpenAI account for local live validation; do not require an Anthropic subscription.

## 6. Prove the integrated work before calling it complete

Establish a repeatable acceptance suite and save an evidence report with exact versions, commands, run IDs, relevant screenshots/recordings, measured timings, tolerances, and remaining limitations. Include tests that can fail for meaningful reasons:

- Execute the cross-domain studies above through the real adapters, with scientific reference comparisons and at least one deliberately invalid input and one failed hypothesis. Vary a physical parameter and show the expected measurable change. Compare rendered entity positions/fields to solver outputs at several times.
- Benchmark the chosen renderer in the installed Windows app against the starting viewer using comparable scientific state and camera views. Record image quality, frame times, resolution, RAM/VRAM, and behavior while a solver also runs. Demonstrate an extended trajectory that exceeds the old retained-frame/short-playback constraints, can be scrubbed throughout, and continues through new computed states; distinguish physical duration, elapsed compute time, stored samples, and exported movie duration in the evidence.
- Export and save a real simulation video from the installed app at two resolutions and two playback speeds. Inspect actual file dimensions, frame rate, duration, and selected start/end states; play the files in an independent video player. Verify slower playback has the expected duration without changing numerical results, and verify navigation, cancellation, completion notification, and the final save/download action. Do not mark export complete based only on a generated filename or successful encoder exit.
- In both simulation and illustration 3D viewers, exercise mouse/trackpad and on-screen zoom, pan, tilt/orbit, fit-selection, reset, and saved views at molecular and large scene scales. Through ordinary chat, change color/contrast, highlight a selected entity, and change camera angle. Verify the requested visual result and undo/redo, unchanged solver-run counts and numerical-result hashes, and preserved presentation settings after navigation/reload. When updating a Blender still, verify that only a render job is created and that the underlying scene/run is reused.
- Have the AI inspect actual retained frames, request an additional view or instrument result, and ground a conclusion in the corresponding numerical evidence. Demonstrate an uncertainty or discrepancy being handled rather than simply producing a confident narrative.
- Demonstrate scheduled visual monitoring during a real run that exposes intermediate output, including an informative event, a recorded image observation, a quantitative cross-check, and an evidence-linked follow-up. Verify monitoring survives navigation and obeys cancellation and usage limits. Compare against numerical analysis alone on a selected benchmark and report where visual inspection added useful information, missed an event, or produced a false alarm.
- Complete a research session spanning multiple meaningful tool actions and at least three evidence-driven design/run/review cycles, including a recovered failure and a retained revision. Demonstrate a forced context-compaction boundary without losing earlier constraints or duplicating the run. Demonstrate pause/resume, timer expiry, timer-off, and restart recovery using deterministic delayed fixtures where possible and at least one real integrated workflow.
- Exercise long-running behavior beyond the old per-request/short-session ceiling. Use deterministic tests for the full configured deadline semantics and a documented real extended session. Do not leave unbounded paid test jobs running. Respect existing spending limits and record actual usage.
- Start a delayed chat/model request in project A, navigate through all views and project B, allow it to finish, and verify no focus theft, missing status, wrong-project output, or duplicate request. Repeat with solver, render, and automatic explanation jobs, then with reload/disconnect/reconnect and worker restart. Test the spinner, unread completion dots, Stop, and multiple concurrent project jobs.
- With model A formerly saved in Settings and model B/effort X selected in chat, inspect captured request payloads and saved provenance across every model-consuming path. Repeat after provider switching, fresh storage, reload, unavailable-model response, and resume. Use deterministic mocked providers for this matrix before limited live verification.
- Verify installation of a missing engine into its managed environment, an actual scientific smoke test, resource-exhaustion handling, cancellation of the process tree, and checkpoint/restart behavior. Confirm the installed app can find its own dependencies without inheriting a developer terminal's PATH.
- Open the installed application and visually inspect the real saved work, new simulations, chat formatting, controls, and results. Tests passing against a development server do not prove the packaged app works. Retain and reopen the user's existing projects after upgrade.

Do not end at an architectural scaffold or silently reduce this assignment to polish. Track outstanding acceptance criteria and continue through implementation, validation, and local installation. If a criterion is impossible with available data, hardware, credentials, or permissions, identify the exact blocker and deliver the strongest validated scope; do not mark an unexecuted adapter or unvalidated claim complete.

## 7. Finish with Windows installation and a small marketplace release workstream

Coordinate with the existing Codex threads **Overhaul PhaseForge research engine** (`01a08c4b-945c-7f81-b669-05cc20ffeca0`) and **Rebuild DeloresAI marketplace** (`01a086d7-002c-7642-a8fa-9a77edb823dc`). Read their latest state and ask for current ownership, packaging contracts, exact-artifact validation requirements, and the MyCast distribution procedure. This prompt authorizes that coordination and the new Windows release attempt after validation. It supersedes the older Windows/Linux/macOS scope and earlier request not to publish. Avoid conflicting edits with work already underway. Windows is the release target; prioritize the tested x64 machine and do not block delivery on Linux, macOS, or untested architectures.

Build and install the real Windows update here, verify that it is the new build and that existing data survives, then produce immutable release artifacts and follow the current Delores acceptance workflow. Revalidate the exact new source and packaged bytes; never reuse an earlier artifact's acceptance, self-award a verified badge, or weaken the marketplace gate. The September 11 public listing already showed an accepted Windows alpha release. Old failed-candidate reports and old schema documentation may be stale; resolve the current requirements with the Delores owner and live service.

Start with PhaseForge's `scripts/release/README.md` and `.github/workflows/marketplace-candidates.yml` (which has a Windows dispatch option), DeloresAI's `packages/contracts/src/index.ts` and `packages/policy/src/release-review.ts`, and its `docs/operations/MYCAST-MARKETPLACE-2026-09-10.md`. The current accepted precedent is retained under DeloresAI `.local/live-rollout/phaseforge-image13-windows-release-status.json` and its publisher report. Renew malware, dependency/SBOM, secret, native-lifecycle, and exact-material evidence as required by the current contract. Match MyCast's marketplace distribution/update policy rather than introducing a competing automatic updater.

Inspect both the marketplace UI and these references: [PhaseForge](https://deloresai.com/apps/phaseforge), [MyCast](https://deloresai.com/apps/mycast), and [Discover](https://deloresai.com/). At audit time, PhaseForge's discovery artwork used `media/phaseforge/molecular-workbench.jpg`, even though its proper logo exists at `media/phaseforge/icon.png`. Use the established PhaseForge logo as discovery/card/featured artwork. Put exactly two compelling, genuine application screenshots on the app's detail/download page only: choose the strongest completed simulation/research views, with readable evidence and attractive rendering. Do not use a desktop screenshot as discovery artwork or fabricate screenshots of capabilities that did not run.

Match MyCast's clear distribution experience. Remove obsolete alpha/early-preview marketing, repeated placeholder language, and unnecessary extra download gates from the completed release. Publish with the appropriate earned release status under current policy; do not simply relabel an unqualified build stable or hide required cost/privacy information. Present the supported Windows installer directly with its version and verified release checks. Check the download path and installed result after publication. Keep the final report focused on delivered scientific workflows, validation evidence, local installed version, and the actual marketplace acceptance/download outcome.
