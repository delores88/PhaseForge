# Scientific workbench delivery ledger

Implementation started 2026-09-11 from `c3843dc1b82e5e04542a5aa5f9eefa7e9a792766`.
The starting installed application is `7471ff72`, version `0.8.0-alpha.1`.
The complete acceptance specification is [SCIENTIFIC_WORKBENCH_CODEX_PROMPT.md](SCIENTIFIC_WORKBENCH_CODEX_PROMPT.md).

## Mandatory checkpoints

| Requirement | Status | Evidence |
| --- | --- | --- |
| Back up current user data and prove restore | Passed checkpoint | `.local/backups/restore-verification/report.json`: 140 verified files, all 8 projects opened in the separately restored previous runtime; SQLite integrity ok. Current data untouched. Repeat for final release. |
| First real OpenMM molecular lab through ordinary chat in installed Windows app | Passed first checkpoint | `validation/installed-first-lab.md`: actual molecular force integration; not biological efficacy. Fresh variant and other domains remain. |
| Frozen independent numerical criteria and controlled intervention | Passed first study | `validation/molecular-lab.md`, `validation/installed-first-lab.md`; both real ordinary-chat runs pass scalar independent pressure/MSD/RDF and coordinate/hash checks. |
| Detailed animation derived from retained numerical positions | Passed first checkpoint | Actual 201/5,001-state trajectories, smooth installed rendering, explicit units/time; final-slider rounding follow-up retained. |
| AI image observation cross-checked against measurements | Passed first study | Session `3293f233-7dba-41a2-b638-77ecad4fa9ab` received two native PNG inputs and compared them with measured results. Further-view/monitoring checks remain. |
| Navigation, deadlines, cancellation and interruption recovery | In progress | Durable jobs and agent sessions |
| Fresh reviewer-selected scientific variant through ordinary chat | Passed | `validation/reviewer-variant.md`, `.local/science/installed-reviewer-variant/report.json`: fresh 32-atom dilute NVE density response, 27 checks per run and 11 overall; independent ballistic and pressure formulas, actual images; retained input-only repair. |
| Second distinct scientific domain using shared infrastructure | Passed installed workflow | `validation/installed-diffusion-lab.md`: real ordinary-chat control/intervention, all 130 fields independently checked, actual native image delivery, isolated authored NumPy instrument and a real specialist correction. Retained operational failures and pending installed repair/layout recheck remain explicit. |
| Classical mechanics, spatial PDE, molecular and held-out ML studies | Pending | |
| Bundled scientific runtimes without host-Python fallback | Actual component execution passed; installation pending | `validation/runtime-v2.md`: exact source seeds, offline verified copies, real OpenMM/field/mechanics and LPAC generated calculations; negative filesystem/network canaries and unchanged inventories. These receipts do not substitute for the new installed package. |
| Retained experimental molecular structures for illustrations | Source tests passed; user illustration revision pending | `validation/retained-pdb-import.md`: eight durable PDB import tests plus two original parser tests passed. Shared chain coordinates, exact source/receipt pins, bounded bonds and restart/cancellation checks. No whole-cell or biological predictive validity claimed. |
| Resumable fixed solver sweep and accounting across retained attempts | Passed service component | `validation/solver-sweep.md`: actual three-case diffusion pause/resume, unchanged completed bytes, new interrupted-case attempt; final targeted run 7 passed/0 failed/0 ignored. Counts failed/paused/completed/reserved attempt files before admission and during execution; completed labels rechecked against original sweep/project/source pins. Original setup, inventory-race and long-test-path failures remain preserved. Installed ordinary-chat sweep/ML acceptance remains pending. |
| At least three evidence-driven research cycles, recovered failure, compaction | First cycle passed; follow-ups pending | `validation/installed-extended-research.md`: actual corrected numerical instrument, specialist handoff, one genuine compaction preserving goals/evidence/failure/next test; the next experiment is only proposed so far. |
| Real extended session and timer-off semantics | Extended execution and numerical audit passed; further recovery pending | `validation/installed-extended-research.md`: 500 ps, 25,001 states, 496.203 s worker time, Timer Off; every retained frame plus 69 independent force/energy/pressure/MSD samples checked. The 480 s component launch cap was not applied to the installed Off run. |
| Camera controls, presentation edits, undo/redo and persistence | In progress | |
| Comparable renderer measurements | Controlled component comparison passed after repair; native observation pending | `validation/renderer-benchmark-observations.md`: six original pairs found a 48 Hz cadence defect; two fresh repair pairs returned to about 60 Hz with unchanged numerical positions. Different geometry cost, p99 intervals, offscreen-host overhead and unassigned VRAM remain explicit. |
| Video exports at two resolutions and speeds, independent playback | Passed second installed checkpoint | `validation/installed-video-exports.md`; saved 720p/3s and 1080p/6s, independent decode/player, unchanged 100ps source, navigation/unread status and cancellation. |
| Actual Windows isolation boundary for generated experiment code | Passed component and first installed instrument | LPAC/AppContainer boundary plus ordinary-chat job `70b96398-5f42-4ebd-9594-1f013aa289c6`: actual NumPy computations on six pinned diffusion files, 535 ms, exit 0. Component filesystem/network/child/RAM controls retained. Storage is a monitored soft limit, not a hard quota. |
| Provider/model provenance across every path, Markdown, global job status | In progress | |
| Bounded recovery from model output exhaustion | Deterministic integration passed; live revised request pending | `validation/output-recovery-and-car-t.md`: retained incomplete provider receipt, no partial-tool execution, bounded repair and explicit continuation, concurrent-resume and specialist retry guards. The original CAR-T failures were output limits, not input-context overflow. |
| Final installed build and existing-project preservation | Pending | |
| Reopening the saved electron-wave project without a startup exception | Repair verified in copied profile; installation pending | `validation/recurring-startup-repair.md`: missing timeline imports repaired; actual conditional component regression and two copied-profile replays pass with the unchanged 251-frame run and project switching. Full frontend 107/107 and desktop 32/32 pass. Original installed app still needs normal Quit and upgrade. |
| Exact Windows package validation and Delores admission | Pending | Owner handoff received; new exact-material review required |
| Marketplace logo discovery art, two genuine detail screenshots, clear download | Pending | Final workstream |

Do not interpret this ledger as evidence of completion. Replace pending entries with reproducible inputs, run IDs, checker results, installed build IDs and artifacts as each check is performed. Numerical correctness does not establish predictive biological validity.

## Ownership

- Root: durable backend execution and tool orchestration, frontend integration, installation and acceptance.
- Scientific worker: trusted OpenMM adapter and independent scientific checks.
- Chat audit worker: Markdown, provider/model picker and Settings.
- Viewer worker: numerical trajectory viewer, camera and presentation controls.
- Existing overhaul thread: read-only handoff complete; no competing implementation.
- Delores thread: read-only current contract complete; coordinate final release after scientific checks.
