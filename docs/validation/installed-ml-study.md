# Installed ML study acceptance

Preregistered before dispatch on 2026-09-11. The fourth local checkpoint is
`2026-09-11T18:04:28.624Z`, installer SHA256
`c8bab3abd52edb9b2e200971c1e64374e763c553926eba2a2f0956065c904cab`.
The frozen scientific protocol remains `ml-lab-proposal.md`, SHA256
`b923777b120659fb5129ae4025efbd1d3a8e4930c05d27840a9436d7697ac737`.
No acceptance threshold is changed for the installed study.

Use ordinary project chat with the configured OpenAI provider, gpt-5.6-sol,
medium effort and explicit Timer Off. The finite request is the registered
99-run argon pressure study, with a 4096 MiB monitored retained-output limit.
Its actual training, validation, calibration, test, regime and OOD labels must
come from newly executed scientific jobs. The component pilot is not reused.
The internal pilot is retained and counted once. Exact source hashes, whole
condition membership, seeds and candidate choices freeze before labels.

During the fixed dataset sweep, interrupt the installed backend after at least
two completed cases and while another solver is active. First preserve the
completed-case receipt hashes, in-flight checkpoint and full job identities.
Verify the owned child process ends, the runtime marks interrupted work paused,
and explicit continuation preserves source and complete case hashes. Resume the
parent and study using their original Off setting, with any independent
continuation explicitly recorded. Do not alter database state or fabricate a
completion. Retain the interrupted attempt and its actual elapsed cost.

After completion, assemble the source manifest read-only using
`tools/export_ml_check_manifest.py` and check it independently using
`tools/check_ml_study.py`. Require all 99 seed receipts, correct role assignment,
scalar pressure reduction, dataset rows, frozen model/calibration/predictions,
baselines, held-out errors, uncertainty and cost accounting. The setup gate uses
the conservative elapsed setup charge documented in `ml-coordinator.md`;
archival/plot/final reporting time remains separately visible. Failed accuracy
or usefulness gates must remain failed and block automatic model substitution.

The agent must inspect actual generated plot pixels and compare them with the
retained numerical evaluation. A specialist must review the exact evidence,
including any rejection. Inspect/download the actual chart and dataset in the
installed app separately; an API dispatch is not a native click test.

After the study and its evaluation freeze, choose a fresh scientific query and
an explanation-only query through ordinary chat. Verify the accepted model is
reused only within its admitted support, otherwise the scientific request runs
three fresh seeds and reports measured uncertainty. Explanation alone starts
no solver. No query may refit or quietly alter the original evaluation.

Integration correctness, completed evaluation and useful predictive
acceleration are separate outcomes. Neither a solver result nor a surrogate
fit establishes biological or wet-lab predictive validity.

## Fourth-checkpoint attempt: failed before any labels

The ordinary-chat request created project `4ca208ba-a700-4ea3-adc5-32cd34721177`
and session `364b0433-602d-4701-a408-c8daa79e0efb`. Study
`57fdb663-3c82-4d43-894b-57e21ded0974` froze its protocol, then pilot
`c8ad85b7-81aa-4066-968d-00f3aafd0dd5` failed startup. A second retained study
`3a739d3f-2188-4943-94ed-8de18161f327` encountered the same failure in pilot
`1accde4f-f361-4b98-8beb-cc87e9215d30`. The agent stopped after that retry.
Neither attempt produced labels, fit a model, or evaluated scientific usefulness.

The supervisor correctly retained `requirements-science.txt` beside the worker,
but the worker's fresh-directory allowlist omitted that file. The repair adds
only that expected filename; equations, seeds, protocol and tolerances are
unchanged. The repaired solver source SHA256 is
`e8fdc41b071bbea9626598b717f7a6f192f9610bce85b79c29af49ab549c4366`.
The failed studies remain immutable and must not resume under a different source
hash. The next installed study will register a new identity with this source.

The actual repaired service pilot passed protocol freeze, OpenMM calculation and
isolated pressure reduction in 52.55 seconds. Its solver is
`904b2cfd-fc46-4b8e-82d9-0075116b8df0`, with seed 111000 at 220 K and
0.45 g/cm³; the retained 100-sample pressure mean is 165.39684255441952 bar.
Receipt: `.local/ml-fresh-fix/latest-report.json`. This is a component pilot,
not the installed 99-seed study. An earlier test invocation failed on an
unnormalized Windows path before computation and remains in the same evidence
directory. Full installed evaluation and interruption acceptance remain pending.

## Preregistered recovery-method amendment before the next installed study

On 2026-09-11, before any replacement installed study is dispatched, the
interruption method is amended to the application's normal native **Quit**, then
relaunch of the same installed version. A process-tree kill is not used. This
changes only the interruption mechanism; the frozen scientific protocol, source
and membership freeze, seed count, endpoint, tolerances and usefulness gates are
unchanged. The failed fourth-checkpoint studies above remain retained and cannot
be resumed under a different solver source.

Bind the next installer/backend/UI/runtime hashes and the actual selected
provider/model/effort/output cap before the new native-chat message. Keep Timer
Off explicitly selected and the retained-output limit at 4096 MiB. The earlier
fourth-checkpoint model choice is a historical configuration; record the actual
new selection before dispatch rather than assuming Settings or an old session
supplies it. Use a fresh project and a new immutable study identity.

The driver must observe **at least two completed dataset cases plus another
active solver case** during the fixed dataset sweep before choosing Quit. Count
unique case IDs and each internal-pilot case only once. Preserve completed-case
input/output hashes, sweep progress, the active solver's job/attempt/process
identity and its last committed checkpoint. Quit through the ordinary native app
action, observe application and owned-worker exit, and retain the actual stop
events. If the active solver finishes normally before Quit interrupts it, record
that attempt honestly and repeat at a later active case; do not claim interrupted
solver recovery from a completed case.

Relaunch the same installed payload, inspect the retained paused/interrupted
state, and explicitly resume through the normal controls with the original Off
policy. Require matching source pins, complete-case reuse without duplicate
labels, retained partial-attempt lineage/cost, and no duplicate paid generation
for a completed provider receipt. Do not alter database rows or manufacture job
states. A graceful Quit check establishes the application's supported shutdown
and recovery path; it does not by itself establish sudden power-loss tolerance.

The prepared normal-chat dispatch is:

> Build and evaluate the complete registered argon-pressure surrogate study with
> all 99 actual seed runs and the frozen training, validation, calibration, test,
> regime and out-of-domain protocol. Use new numerical labels, retain all source
> and dataset evidence, and keep the output storage limit at 4096 MiB. Explain
> the pilot timing estimate, then continue the full study. Keep a failed accuracy
> or usefulness result as a rejection and use the actual measured solver fallback
> where required. Inspect the final chart and have a specialist review the saved
> evidence. Report integration completion separately from predictive usefulness.

After recovery and completion, run the already specified independent 99-seed/
33-condition checker, inspect the actual chart/dataset through the native app,
verify native model image-input acknowledgements, and perform the preregistered
fresh scientific and explanation-only queries. This is a prepared procedure;
dispatch still awaits the new installed identity and root GO.
