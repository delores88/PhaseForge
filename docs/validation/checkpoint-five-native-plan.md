# Next installed checkpoint: native scientific illustration acceptance

Prepared after source 0.9.0 build and completed renderer-component measurements. This plan has not been executed. No new paid request, UI input, installation or app restart is authorized by this document itself; the coordinator will provide GO after the concrete package and normal application Quit are available. The prior CAR-T plate remains retained evidence and does not satisfy the user's clarified scientific 3D request.

## Entry and preservation

Select the freshly returned native PhaseForge window, record its actual build/version, main/backend process IDs and API health, and compare retained job IDs/states against the 47-job pre-recovery snapshot. Do not reset a profile or overwrite a visible user draft. The recurring startup exception is now diagnosed: GenericViewport's retained-frame footer omitted imports for three timeline helpers, producing `ReferenceError: TIMELINE_SLIDER_MAX is not defined` when the saved electron-wave project opened. The corrected production UI passed two exact copied-profile replays; see [recurring-startup-repair.md](recurring-startup-repair.md). Verify the installed update restores that same project and its 251-frame timeline, reaches the retained endpoint, and permits switching to CAR-T and another legacy project without a boundary fallback. Confirm new renderer logging/reload controls are present without deliberately crashing the installed app.

Use the existing `T Car therapy` project 8967071e-7425-4c8e-b283-09e76cb34338 for the user's revision. Preserve illustration 60f4d832-cc44-4ea5-af5a-b40f56796e7f and generated annotated plate 93c5db8e-a6c7-4771-badf-7becc237981e. Confirm the latter's actual 2048 × 1152 PNG is now a selectable, hash-verified preview with PNG/SVG/caption downloads and retained source files accessible.

## Ordinary-chat scientific revision and background work

Before submission, inspect Settings for provider/account controls and absence of a global model selector. Return to chat and inspect the actual available model list, six-position ordering where six suitable models exist, gradient control, selected model/reasoning, and custom time field. Use the coordinator's specified model and a bounded request budget; never substitute a different provider silently. Record actual requested/resolved model and deadline from the resulting session, rather than inferring them from a label. Verify Settings navigation preserves the chat selection and unsent draft.

Enter one normal chat revision of the existing work: a realistic scientific 3D cellular/molecular CAR-T illustration with useful leader labels, natural materials and depth; preserve the earlier plate; avoid a marketing panel/banner layout. Ask it to use available trusted experimental structure data where relevant, retain the downloaded structure/provenance and exact source coordinates, and clearly label conceptual cellular context versus source-derived molecular detail. Let the agent inspect the trusted import tool's actual metadata and scientific scope; do not predetermine unsupported biological claims or count a photorealistic surface as atomic accuracy. The final prompt will incorporate the import tool's stable contract and the user's existing clarification without asking them again.

While this real render/import task runs, navigate to a different project/view, scroll retained work and return. Observe the sidebar spinner and progress/tool handoffs. Leave the active project before completion and verify completion does not steal focus; confirm its unread cue appears and clears when selected. These interactions must not cancel the task. Inspect the final actual PNG and editable 3D model, job artifacts/hashes, declared conceptual versus structure-derived content, tool receipts and solver count. A static illustration should create no scientific solver run.

## Presentation and persistence

Use at most two additional ordinary-chat requests unless a concrete failure requires coordinator review: first a visual-only change to color/contrast/highlighting and camera of the saved 3D subject; then an explicit high-quality rerender of that saved scene. Verify presentation revisions and stable subject IDs, unchanged numerical/source-coordinate digests, zero solver reruns, and immutable rerender lineage. The first request should reuse the saved scene and the second should render its selected presentation rather than rebuild unrelated geometry.

Exercise actual viewport zoom, pan, orbit/tilt, roll, fit selection, reset and saved-view recall. Record physical camera values where exposed. Make a small presentation change, undo it, redo it, navigate away/back and reload only the interface using its supported control. Verify the durable camera/style and the user's unsent text survive the intended transitions. Check controls remain reachable at the available narrow pane width and the send/stop circle stays at the right edge. Do not count a test-harness callback as installed evidence.

## Efficient remaining checks

Open an existing completed numerical trajectory and a retained diffusion field to confirm the new build still displays their actual data, accessible timeline/legend and export/download controls. This is a viewer smoke check, not another numerical or performance benchmark. Open the completed real ML plot when the coordinator's study is available; require the actual plot and numerical/provenance downloads, not just result JSON.

Any additional fresh intent cases will be named and budgeted by the coordinator after the CAR-T result. Distinguish illustration, numerical experiment and unsupported scientific scope from the actual requested output and created job kinds. Do not manufacture a solver run to demonstrate a static illustration, and do not treat static animation as an experiment. Capture unexpected errors with the new local diagnostics, preserve failed attempts and report remaining gaps explicitly.

## Concrete CAR-T revision entry

The trusted import contract and scientific reference were reviewed in [retained-pdb-import.md](retained-pdb-import.md). After GO, use ordinary chat in the existing CAR-T project with this request:

> Please revise my earlier CAR-T illustration into a realistic scientific 3D view of a CAR-T cell engaging a CD19-positive cancer cell, with useful readable leader labels and editable 3D output. Make the cellular and molecular subject the main visual, with natural materials, depth, thoughtful lighting and clear contrast. Keep the earlier labeled plate as a saved artifact. This is a scientific illustration request, not a dynamics experiment.
>
> Include a scientifically sourced molecular detail of the CD19–FMC63 binding complex using the official PDB 7URV structure if it can be acquired and verified. Preserve the original structure file, its provenance and common coordinates; keep the interacting complex together rather than moving or independently fitting its chains. Clearly distinguish this source-derived molecular detail from the conceptual cells, membrane placement, missing CAR domains and any illustrative magnification. Do not invent missing atoms or imply that the image demonstrates treatment efficacy. Produce the detailed Blender still and editable scene, inspect the actual rendered image, and correct obvious visual or labeling problems before presenting it.

The agent should naturally use `acquire_data` to retain the official PDB, then `import_structure` with the exact completed data job/path/SHA, a name and explicit angstrom units. Verify the resulting retained receipt and `structure_id`, and that the complete complex is bound to one scene node. Expected source feasibility is 3,460 atoms: author chain C has 1,701 and D has 1,759, at reported 3.05 Å experimental resolution; unresolved/absent domains remain conceptual. These are import/provenance expectations, not a newly executed biological experiment or a claim that the entire cell has that resolution.

After the new saved scene is selected, the two presentation requests should be ordinary, specific visual instructions grounded in actual visible subject IDs: adjust one chosen color/contrast/highlight and camera; then explicitly rerender the current saved scene at the selected output size. Use the resulting receipts and unchanged source coordinates to verify reuse. Do not submit placeholder IDs or modify a different user's active draft.

If supported installed diagnostics expose actual viewer cadence while the coordinator's solver is already working, record that bounded native observation with its current build, viewport, active job and device state. This supplements the component evidence; it does not trigger another broad benchmark series or a new solver solely for graphics testing.
