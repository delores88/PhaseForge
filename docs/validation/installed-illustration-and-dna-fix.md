# Illustration acceptance and discovered DNA primitive defect

The third installed Windows checkpoint received an ordinary chat request in the separate **Scientific illustration** project (`a04a6c59-bc2c-4966-bf8c-214a060edeee`), using `gpt-5.6-sol`, medium reasoning, and a 300-second deadline. Session `1421a20d-6cc3-4e8e-823c-d9315ba714e5` requested a conceptual DNA illustration with separate blue/gold backbones, contrasting bases, dark background, editable GLB and BLEND, an actual image inspection, and no solver.

The first two completed Blender renders failed the requested palette. Both backbones shared one material because the DNA primitive ignored `parameters.colors`, `count`, and `thickness`. The agent's native image observations correctly identified the mismatch, retained the failed images, and replaced the primitive with separately authored sampled curves. The third job, `053fec59-dc38-493c-ba47-ac960707fe55`, visibly has distinct blue/gold strands and four contrasting base colors; its explanation acknowledges the curves' mild faceting. All three PNGs have native provider image-input acknowledgements. All PNG/GLB/BLEND hashes and sizes were independently checked. There are zero solver jobs in this project. Evidence is in `.local/render-acceptance/installed-illustration-01/initial-report.json`, with full job records and geometry signatures retained beside it.

Native navigation to Settings succeeded while the rendering job ran; the sidebar spinner changed to a blue completion dot without moving the current view. In the initial GLB, zoom, pan, tilt, roll, home fit, save camera, and saved-camera recall visibly worked. Saved camera source coordinates, target, and nontrivial up vector were confirmed through the read-only presentation endpoint. Further presentation-only chat edits, undo/redo and navigation persistence checks remain pending: native input paused when local user activity was detected. These are not reported as passed.

The next-source DNA fix makes the schema and renderer agree: palette entries 0–1 select distinct backbones, later entries repeat over successive paired base halves, count specifies exactly 1–160 base pairs, and thickness specifies the actual backbone tube radius. Unsupported values fail admission rather than being silently clamped. Strand tessellation is independent of pair count and uses 128 segments per turn with smooth surfaces and capped ends. Component identifiers and conceptual provenance are retained.

The exact original failed request was rendered with the fixed worker at 1200×1200, 160 Cycles samples, using the local OptiX device. The PNG was visually inspected. A separate process opened the actual saved BLEND and verified 50 geometry components (two strands and 48 halves), all six requested material colors, and the maximum mesh distance from an independently sampled analytic helix: 0.1900043 against the requested radius 0.19, within the declared 1% tolerance. Evidence: `.local/render-acceptance/dna-primitive-fix-01/geometry-check.json`, `render.png`, `scene.glb`, `scene.blend`, and `renderer.json`. Three pure Python contracts and four Rust scene-validation tests passed. This source fix has **not** yet been installed, and neither illustration demonstrates atomistic DNA accuracy or biological simulation.
