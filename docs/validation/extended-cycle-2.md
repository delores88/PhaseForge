# Second measured research cycle: replicated, equal-lag displacement

Preregistered after inspecting the first extended study and its actual specialist
review, before this follow-up's labels. The first 500 ps realization showed that
early/late slopes of single-origin MSD were unstable; those slopes do not measure
a change in diffusion. The follow-up tests whether displacement over the same lag
is similar in early and late production windows across independent seeds.

Use the next installed build after the fresh-directory and decimal-preservation
repairs. Bind its exact executable, scientific worker and bundled runtime hashes
before dispatch. Keep the original project
`6989338a-79a8-46f5-9f49-9f4bbf8366f3` and reference original solver
`62bdf4b7-039b-48b5-8024-6f6feabf695e`, corrected instrument
`4522297c-c635-4b59-95e9-521d64e7d1fb`, and specialist
`31d8e5ff-9a61-4a6e-830d-4c98b2465a08`. Their recorded failures and limitations
remain part of the conversation. Use ordinary chat and explicit Timer Off for
this finite requested protocol; stop after reporting the results and proposing
the next evidence-driven test.

Run exactly three new OpenMM argon cases using seeds 314160, 314161 and 314162.
Every case uses 32 atoms, 120 K, density 0.8 g/cm³, Langevin friction 1/ps, one CPU
thread, timestep 1 fs, 200,000 integration steps, sample interval 100 and chunk
size 100. Each trajectory contains 2,001 saved states from 0 through 200 ps.
No first-study states become labels for this new seed ensemble. A 512 MiB
retained sweep budget is sufficient for the declared small trajectories; preserve
resource failures if actual use exceeds it instead of deleting evidence.

An isolated instrument reads the recorded unwrapped coordinates and time/step
arrays. For each seed and lag 1, 5 and 10 ps, compute the mean squared particle
displacement over all saved time origins for which both endpoints lie in the
window 50–100 ps, then separately 150–200 ps. Endpoints are inclusive. Determine
membership from integer integration steps to avoid a floating-point boundary
selection error. At a lag of 10 ps, the first window has origins 50 through 90 ps
inclusive, or 401 recorded origins. Retain actual counts for every window/lag.
Do not wrap coordinates before subtraction or silently remove center-of-mass
motion. Report this raw-displacement definition and its finite-system limitation.

Primary summaries are each seed's early value, late value, paired difference
and late/early ratio for each lag. Across the three independent seeds report
mean, sample SD, SE and the paired mean-difference interval using Student t with
2 degrees of freedom (critical value 4.302652729911275 for 95%). This small-sample
interval assumes an approximately normal seed-level difference; overlapping
time origins and individual particles must not be treated as independent seeds.
Also report temperature and potential-energy window means as descriptive checks.
No acceptance tolerance for biological prediction or a universal stationarity
claim is introduced by this protocol. An interval spanning zero does not prove
equivalence. A discrepancy is an observation to investigate, not a failed
software test to tune away.

Keep each per-seed instrument within the existing 32-import limit by importing
that seed's registered chunk files and metadata only; combine the three retained
summary artifacts in a separate isolated aggregation. Retain exact source pins,
code, numerical tables and any corrected attempts. Independently rederive a
selection of equal-lag displacements from raw coordinates outside the agent's
analysis before crediting the cycle. Verify every source file hash.

Have the agent inspect actual retained image pixels from the recorded simulation
and compare their interpretation with numerical evidence. The image alone cannot
measure stationarity; report what the view does and does not reveal. A specialist
must review the completed seed-level comparisons and uncertainty using their
actual evidence IDs. Its conclusions should inform the third experiment's
preregistered question. Do not run a guessed third test before this review.

All execution, analysis, image-observation and review results remain pending.
