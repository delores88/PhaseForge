# Workbench actions and actual user outcomes

Current release acceptance is dependable research software, not success on an extreme collision. This map separates observations of installed0.9.1, source behavior, and verification still required. No new scientific study or provider request was started for the installed-app inspection.

## Installed application observations

Inspected the actual Windows app and its local interface at `http://127.0.0.1:7332/` on2026-09-12. The installed executable remains0.9.1. Its profile contains18projects; the user's `Blackhole collision` project is `3a8b52bc-5669-417c-a837-f4cf5d12a1d2`.

| Visible action or state | Actual observed outcome | Implication for the update |
| --- | --- | --- |
| Native Blackhole collision workspace | Chat's latest answer describes saved playable HTML job925a7791…, while the selected main panel shows the older `Incoming shock-limit tidal distortion` Blender still. | Completion, selected output and discoverability must agree; a text promise is insufficient. |
| Results in that project | Opens `Your findings will appear here` despite `Scientific lab3` and completed laboratory jobs. | Legacy equation findings and laboratory results read different stores. A shared exact-result navigator is required. |
| Experiment | Opens a distinct equation-build workspace with no setup for this project; it does not display the newer laboratory jobs. | Keep the useful equation workflow, identify its purpose and connect its completed results. Do not rename it and assume the data problem is solved. |
| Experiment with no conversation model selected in this browser | Build buttons disabled; model says `Choose a conversation model`, but old explanatory copy incorrectly says to connect a provider. | Report the actual missing model/account condition. |
| 3D & fabrication | Opens a separate studio with scene/CAD/PCB tools, Blender ready, CAD/KiCad not installed, source import and renderer controls. Current lab illustration is not automatically a studio design. | Make scope and missing dependencies clear; keep scientific-lab results accessible from this view. |
| Render in Blender with no usable studio source | Disabled with `The experiment has no single recoverable scene or molecular source. Choose its original design or structure.` | This is an explicit unavailable outcome, not an inert control. Preserve it. |
| Setup without equation manifest | Shows `No setup yet` with build/equations/import instructions and an Import manifest button. | Contextual tool visibility can reduce clutter, but the supported creation path must remain. |
| Open project, Results, Experiment and Studio | The Blackhole collision unread dot remains visible; no new jobs ran during these clicks. | Verify durable read acknowledgement in the updated app. |
| Previously selected Viral envelope equation experiment | Actual201-frame scene, playback controls and a completed lineage entry are present. | Historical equation playback must remain reachable after navigation changes. |

Computer-use captured the installed native workspace successfully, but two later handle-based input attempts returned unusable/missing-window errors. This is an automation limitation with unknown cause, not evidence that the app crashed. Remaining installed control observations used its actual local web interface. Original projects, numerical artifacts and job execution states were not changed.

## Updated application ownership map

| Surface/action | Source and state read | State written / expected outcome |
| --- | --- | --- |
| Project navigation and background-job links | Project ID, `liveRuntime` laboratory records, legacy runs, per-record completion/read timestamps | Capture the selected project/job, acknowledge the appropriate completion and open that record. Navigation must not start/cancel an execution. |
| Scientific lab | `ResearchWorkbench` selectedLabId; `initialLaboratorySelection`; laboratory viewable artifacts | Select a saved output once on entry; preserve the inspected iteration through polling. `LaboratoryViewer` chooses the correct retained-data renderer. Show failed/partial status above usable output. |
| Results / open latest / specific result | `completedWorkspaceResults` combines project-scoped laboratory jobs and equation runs; keys include source type and ID | Resolve against current project, load the exact record, then open laboratory viewer or equation findings. No stale selectedRun fallback. |
| Horizontal lineage | `laboratoryLineage`, selectedJobId and project scope | Chronological left-to-right cards, newest initially at right; explicit earlier selection survives new completion/polling. |
| Equation editor / experiments / setup / measurements / evidence | Current project's manifests and legacy run snapshots; `workspaceTools` includes applicable entries | Retain real manual/build/run and findings workflows. Missing model errors must differ from missing provider account; source already uses the actual model-selection error. |
| CAD, PCB & scene tools | `StudioWorkbench` project-owned designs, jobs, assets and current conversation model | Build a design or inspect retained studio output, with explicit source/dependency availability. Global Results still opens laboratory/equation outputs. |
| Notes & sources | Project research plan, source records and proposal callbacks | Read/write project notes and perform explicitly requested source/proposal actions; no automatic experiment solely from entering a view. |
| Agents / job history | Project-scoped laboratory summary, full recorded activity on expansion, legacy research sessions | Exact job inspection, truthful state/progress, stop/pause/resume, and actionable attention. A completed process is distinct from a fulfilled or scientifically validated result. |
| Recover retained NR output | Original inactive job, immutable execution/diagnostic inventory; new durable recovery operation | Prompt acknowledgement; background copy/verification, visible queued/verifying/terminal status, exact-operation cancellation. No numerical rerun or timer extension. |
| Needs direction actions | Server-listed attention actions and exact evidence IDs | Revise request focuses current composer; capabilities opens engine scope; evidence opens actual related records. Unchanged capability gaps cannot purchase another Resume call. |
| Explain / next steps for selected lab result | `LaboratoryResultActions`, full completed source, conversation model/effort/time snapshot, request ID | Read-only review reply in chat and progress in Agents; exact source identity preserved. Uncertain submission retains the same request for explicit retry. |
| Chat send / steering / Stop | Project request gate, active session, model selection and persisted work-limit choice | New session or steering at the next boundary; Stop targets that session. Navigation/reopen must retain ownership and result context. |
| Playback / camera / presentation | Verified retained numerical frames or authored geometry and separate presentation | Inspect real saved values; pan/zoom/fit/tilt where meaningful. Color/contrast/camera changes do not rerun science. |

Primary implementation references: `frontend/src/components/workspace/ResearchWorkbench.jsx`, `WorkspaceResults.jsx`, `LaboratoryResultActions.jsx`, `LaboratoryActivity.jsx`, `frontend/src/lib/workspace-results.mjs`, `workspace-navigation.mjs`, `laboratory-read-state.mjs`, `modelSelection.jsx`, and backend laboratory/agent lifecycle modules. These code relationships are not substitutes for the actual workflow checks below.

## Small integrated acceptance set

1. **Supported result:** reuse verified heat/flow and CUDA diagnostic outputs; confirm exact visible data, playback/inspection, ordinary explanation and quantitative analysis, with selected model/time. Actual CUDA explanation and quantitative Analysis passed; see `nr-ui-results-2026-09-12.md`. Dedicated result-action acceptance remains pending final source.
2. **Stopped attempt:** use the actual failed gauge result and deterministic local failure fixtures. The viewer must say failed/partial; the orchestrator must stop repeated errors/no progress, retain receipts and expose concrete options. Unsupported scope cannot silently become a still or another engine.
3. **Navigation and recovery:** recover the same completed CUDA job with prompt acknowledgement while navigation remains responsive; prove immutable input/result/deadline, exact cancellation, reload and selection/read state. The original50-second recovery POST timed out at30seconds and remains a failed receipt. Corrected background-operation acceptance is pending.
4. **Packaged Windows delivery:** exact final source/installer, required native checks, actual local backup/install and ordinary marketplace admission. Future engine portability does not imply another platform release.

Reopen any affected acceptance item when an ordinary path reveals a defect. Do not infer reliability from test counts, bypass scientific checks, broaden this into open-ended studies, or mark delivery complete before installation and marketplace acceptance.

## Final source-UI outcome

The final source walkthrough on build `OwHJ3IJwEWTrjPSjKTWA_` passed the
previously pending actual attention and dedicated-review actions. The existing
paused gap exposes useful capabilities, its exact saved evidence and a draft-
preserving revision action without an unchanged-gap Resume. The one dedicated
CUDA result explanation completed with its exact source/model/time snapshot,
two compactions and no numerical rerun. Agents, Notes and another project
remained usable while it ran. The completion dot appeared while away and
cleared durably when the project was opened. The recovery-completed state was
also visible on the original solver card.

See `nr-ui-results-2026-09-12.md` for exact jobs and receipts. Its measured
1,050-word default explanation remains a concision limitation; no follow-up
request was issued to tune it. Earlier source/installed-profile compatibility
observations remain separate and preserved. Windows packaging, actual local
installation and marketplace admission are still required.
