# Installed chat intent-routing acceptance

Preregistered on 2026-09-11 before dispatch against the next installed Windows
checkpoint (the fifth native path). No cases below have passed merely because
the source prompt or tests changed. This document records the expected behavior;
append actual results and failed attempts after execution without rewriting the
criteria to match the output.

## Prior observation and the clarified request

The fourth checkpoint retained the user's CAR-T request in project
`8967071e-7425-4c8e-b283-09e76cb34338`. Third session
`59607b5d-b5e4-4f2e-9b05-c486d1d021ab` completed a Blender scene and subsequent
annotation. The exact job IDs, original PNG/GLB/BLEND and annotation hashes are
recorded in [output-recovery-and-car-t.md](output-recovery-and-car-t.md).
Root inspected the PNG and observed a step-card communications layout with
labels and an all-caps headline. Earlier sessions exhausted their output cap;
that failure was not an input-context overflow. These are prior observations,
not results of the new acceptance cases.

The user subsequently clarified that the intended output is realistic scientific
3D CAR-T imagery, at an appropriate cellular/molecular/atomic scale, with useful
labels. A communications audience does not imply a marketing plate. Preserve the
previous scene and graphic as historical artifacts, but do not count them as
fulfilling that clarified intent. Neither the plate nor a more realistic render
establishes biological accuracy or predictive validity. Structure-specific claims
require retained, verified structural data; otherwise the geometry must be
identified as conceptual at its stated scale.

## Installed identity and evidence rules

Before the first message, retain the installer, executable, backend, UI bundle and
runtime-seed hashes, build metadata, current provider/model/reasoning selection,
output cap, repair allowance and explicit next-request time budget. Record the
ordinary native chat submission and its project, message and session IDs. A direct
API submission or component test cannot substitute for this native entry-path
check. Read-only API/job inspection may collect evidence after native submission.

Use one fresh project for the ordered cases below. Also perform the CAR-T revision
in the existing project so that history and its earlier graphic cannot silently
override the correction. Do not insert engine/tool names or hidden routing flags
into submitted messages. The stated request and prior clarification are the
inputs being tested. One concise clarifier is acceptable only when a missing
choice materially affects the deliverable; a resolved preference must persist.

Before each message, record existing jobs and immutable artifacts. Afterward,
record only jobs caused by that message, including descendants, tool-call names,
effective inputs, selected model/effort, deadlines, process receipts and artifact
hashes. Distinguish numerical experiment code from generated scene/annotation
code: rendering or annotating a static image is computation, but is not a
scientific numerical experiment. No-solver checks must include sweep, ML and
generated numerical paths, not just jobs literally named `solver`.

For a numerical result, require real saved values, time/units, a supported model
and execution receipt; animation must follow retained states. For a Blender
illustration, require actual renderer provenance and completed PNG/GLB/BLEND
artifacts, inspect the PNG and interactive geometry, and retain their hashes.
When the agent claims to have inspected an image, require the completed native
provider-request acknowledgement to contain the exact image hash/path, not merely
an observation entry or a journal containing pixels. This checks that the model
received the image; it does not independently establish biological validity.

## Representative normal-chat cases

These messages may be adjusted only to substitute an actual selected result ID or
a concise answer to a necessary clarifier. Record exact submitted text. Preserve
the intent and criteria before observing the result.

| Case | Native chat message | Required result |
| --- | --- | --- |
| I1: numerical simulation | “Simulate a small argon system at 120 K and density 0.8 g/cm³: 32 atoms, a 1 fs step, 500 steps, random seed 314159. Show the measured energy and pressure over time and animate the computed particle motion.” | A supported, validated numerical configuration is executed and its actual states and measurements are retained. The response identifies the finite protocol and limitations. No orbiting decorative substitute or unexplained time mapping. |
| I2: explanation after simulation | “Explain what this result tells us and which assumptions limit it. Use the saved results; do not run another experiment.” | The selected I1 evidence is used and cited. No new solver, sweep, fit, generated numerical experiment or render. The open simulation view does not force a new experiment. |
| I3: static scientific 3D without naming the renderer | “Create a detailed static scientific 3D illustration of a CAR-T cell contacting a cancer cell, with a labeled receptor–antigen close-up. This is for medical communications. Make the cells and molecular interface the main subject, with no headline, banners or step cards. Use verified structures for any structure-specific claims; identify conceptual geometry and scale limits otherwise.” | Actual Blender rendering and editable 3D artifacts, with the subject and useful labels visibly present. No new numerical biology experiment, marketing plate or invented atomistic accuracy. The words ‘medical communications’ do not override the requested scientific scene. |
| I4: edit the saved view | “On this saved 3D result, make the T cell blue and the cancer cell gold, rotate the camera to show the contact surface, and keep the labels. Save that presentation.” | The explicitly selected I3 result receives a retained presentation change. Camera/material settings, target identity and before/after values are inspectable and survive leaving/reopening the view. Original scene input and scientific artifacts stay hash-identical. No new numerical solver or regeneration of the scientific scene. An explicitly needed additional view render must be identified as presentation-only and linked to the original. |
| I5: return to numerical work | “Return to the argon experiment. Run a new variant at 150 K with the same other settings, and compare its measured result with the 120 K run.” | A new numerical job records the exact intended temperature change and original controls, retaining its own lineage and measured output. The most recent illustration or camera tab cannot cause a new illustration or silently select another experiment. |
| I6: explicit graphic intent | “Make a separate labeled three-step explanatory graphic using the existing CAR-T illustration. For this one, a title and step cards are appropriate. Keep the scientific 3D scene as a separate artifact.” | The explicit request may produce a communications graphic with retained source lineage. It remains distinct from the original scientific 3D scene. No new numerical experiment, and the prior preference against cards is correctly scoped to the previous deliverable rather than blocking this explicit switch. |

The numerical settings in I1/I5 test routing and real computation, not an
equilibrium argon validation benchmark. Use the existing independent numerical
checks for the frozen scientific gates; do not infer scientific validity from a
successful chat route alone.

## Existing CAR-T project revision

In the existing project, select the actual saved 3D illustration and submit:

> Revise this into the scientific 3D illustration I meant: a realistic cellular
> view of CAR-T contact with a cancer cell, plus a clear molecular-interface
> close-up and useful labels. The cells and molecular interface should dominate
> the scene. Remove the marketing headline and step-card layout. Use verified
> structural data for structure-specific details when available; otherwise label
> the conceptual geometry and scale limitations. Preserve the earlier plate as
> a separate artifact. I want a static scientific illustration, not a simulated
> treatment experiment.

Require actual subject-centric Blender artifacts and inspect their imagery and
geometry. The scene may be conceptual where verified structures are unavailable;
it may not fabricate accurate atom coordinates or convert lighting realism into
an accuracy claim. Record any verified data identifiers, acquisition source,
exact retained hashes, coordinate units and mappings used. The labels should
remain useful and legible. Zero new scientific solver jobs are expected.
This revision passes only when the requested scene is delivered, not when a
model acknowledges the correction or restyles the same step-card plate.

## Stale state, navigation and switching checks

Record the visible tab, selected result, project and any retained legacy mode
fields at each submission. Submit I2 with the numerical view open, I3 after I2
without resetting the conversation, I4 after explicitly selecting its saved 3D
target, and I5 with the illustration view still open. Inspect outgoing payloads
and effective session inputs: stale `research`/`auto_run` flags or the latest
unrelated result must not change the intent. Do not edit a stored mode merely to
make a case pass. If a legacy control no longer exists, record that fact and use
the retained conversation/view state that exists in the installed app.

During one actual job, navigate to Settings and a second project, then back.
Require responsive navigation, continued backend work, a spinner based on actual
job state, and a completion indication without changing the user's current
project/view. The next-request model/time settings may change while the active
session's original selection and deadline remain immutable. A selected result
from the first project must not silently attach to a message in the second.

For every case, compare the answer with its actual tool receipts. A successful
utterance cannot conceal missing artifacts, unfinished jobs, a contradictory
route, or a skipped image inspection. Retain provider/output-limit failures and
subsequent attempts. A source-only recovery test is not an installed recovery
pass, and a new UI error boundary is not proof of the previously reported crash's
root cause. Report any unsatisfied case as failed or pending with its evidence.

## Relationship to the installed ML gate

This routing preregistration does not replace or relax
[installed-ml-study.md](installed-ml-study.md) or the frozen
[ml-lab-proposal.md](ml-lab-proposal.md), SHA256
`b923777b120659fb5129ae4025efbd1d3a8e4930c05d27840a9436d7697ac737`.
The full installed ML driver must interrupt during the fixed dataset sweep only
after **at least two completed dataset cases and while another solver is active**.
The internal pilot is counted once by its retained ID; a component pilot cannot
substitute for installed labels. Bind the new installed hashes before dispatch,
retain complete-case hashes and the active attempt, preserve Timer Off and
verify completed-case reuse after explicit recovery. No ML dispatch is authorized
by writing this document; the root's installed-checkpoint GO remains pending.

## Results ledger

All I1–I6 cases, the corrected existing-project CAR-T revision, navigation/state
checks and the fifth-checkpoint installed recovery path are **pending** at
preregistration. Append installed identity, exact messages, evidence locations,
case outcome and any failure or limitation here after the actual tests. Prior
fourth-checkpoint artifacts and source tests remain separately identified.
