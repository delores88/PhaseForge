# CAR-T output access and interface recovery

Observed 2026-09-11 against installed checkpoint four; source checks followed before checkpoint five packaging.

## Original live incident had no captured stack

The native Electron window showed Next's generic client-side exception page. Main PID41116, renderer44412 and backend57500 remained alive. The existing shell had no renderer console logging and no effective Ctrl+R or Ctrl+Shift+I handler after removing its menu. Restoring the existing single-instance window preserved that error page. No backend restart, profile reset, user message, solver run or render was performed during this investigation.

An independent Edge browser tab loaded the exact installed UI from http://127.0.0.1:7332 and opened the same T Car therapy project8967071e-7425-4c8e-b283-09e76cb34338. The completed chat, retained Blender PNG, editableGLB and47-event session timeline loaded without captured console errors. This does not reproduce or explain the original native exception. Its original JavaScript stack was unavailable. Containment and diagnostic changes below are separate reliability improvements, not a claimed regression fix for an identified cause.

A subsequent exact copied-profile replay reproduced the recurring startup failure and identified a missing import in GenericViewport's retained-frame timeline. The profile actually restored the electron-wave legacy project. The narrow repair, full old stack, two successful production-bundle replays and preserved setup failures are documented in [recurring-startup-repair.md](recurring-startup-repair.md). That later evidence resolves the recurring startup defect; it does not recover the original live incident's missing stack.

## Verified access defect

The agent completed a labeled2048×1152PNG, editableSVG, caption and annotation manifest in generated job93c5db8e-a6c7-4771-badf-7becc237981e. The PNG digest is22f3eb23126796a9ead23c794d75fe218ab16a87d03f3d975bc6f5caffe789b0. The final chat named the files in inline code, while generated jobs were excluded from the normal laboratory viewer. The visible illustration60f4d832-cc44-4ea5-af5a-b40f56796e7f was the earlier unlabeled Blender source.

Completed generated jobs now open a generic output viewer. It fetches the full completed receipt, resolves declared files only against registered artifacts, verifies displayed raster bytes against their registered SHA-256 and size, and exposes direct downloads. All retained source/import/code files remain accessible separately. SVG is downloadable and is not executed as page content. This viewer does not turn a generated file into a numerical trajectory or declare scientific validation.

The actual saved PNG was rendered by the new source React component:2048×1152decoded, original aspect preserved, primaryPNG/SVG/caption/manifest links present, zoom43%→53%→43%reset. Evidence:.local/viewer-acceptance/tcar-output-source.png and tcar-output-recovery.json. These are source component checks, not an installed checkpoint-five acceptance.

The existing labeled plate is retained evidence of what was produced. It does not satisfy the user's subsequent clarification that they wanted a more realistic scientific3D subject with useful labels rather than a communications layout. That visual revision remains separate work.

## Composer and recovery changes

Global CSS gave both composer toolbar groups flex:1. The action group therefore placed Send/Stop at the start of the right half. Scoped rules now keep a fixed-width action group at the far right, with circular38px arrow-up Send and red square Stop buttons, accessible labels and visible keyboard focus. Existing submission and cancellation behavior is unchanged.

The actual source ChatDrawer and ModelPicker, with all application global styles, passed nine desktop document layout cases:320/480/720px chat panes × idle/running/steering. Controls were aligned to the right inner edge with zero horizontal overflow and no overlap with model tools. Stop, update-send, Enter-submit and disabled-empty behavior were checked through the actual component handlers using local mocks. Evidence:.local/viewer-acceptance/composer-layout.json. A separate mobile document viewport check was not completed;320px pane width was measured within a desktop browser viewport.

Scoped React boundaries now contain workspace-view failures and retain a raw-text fallback for a failed Markdown message. A page boundary provides a last-resort retry. Local error records retain bounded exception/stack/component context, and the desktop shell writes rotating renderer logs with credential-token redaction. New Ctrl+R/tray reload refresh only the interface; Ctrl+Shift+I opens local diagnostics. None of these recovery handlers change backend jobs or clear local drafts.

A deliberately injected source-view exception was contained while sibling navigation and a typed draft remained usable. Switching back restored the saved-output view and preserved the draft. This fixture exception is not the native incident. Two desktop tests verified modern/legacy console events, stack retention, redaction, process-exit records, repeated rotation and fail-safe logging. Sixteen targeted frontend tests passed. The Next16.3.3production build exported all eight pages successfully.

## Controlled native recovery attempt, 18:58–19:01 UTC

A fresh API snapshot retained 47 jobs: 33 completed, nine failed, four paused, one cancelled, and none active. It is saved as `.local/viewer-acceptance/tcar-jobs-before-recovery.json`. The original main41116, renderer44412 and backend57500 were alive. Launching the existing installed executable restored its single-instance window17502932 without replacing the process or changing files. That window still displayed the generic exception.

The supported Computer Use target lists exposed no taskbar or notification-tray window, so the application's normal Quit menu could not be reached through a returned target. Windows-key notification shortcuts are prohibited by that skill. The coordinator's separate owned-process stop was rejected by automatic approval review before execution, with no detailed reason beyond blocked by policy; it was not retried through another mechanism. No application restart or native recovery has therefore been claimed. The visible error page and saved data were left intact for normal Quit/relaunch when available. This limitation does not affect the completed source build and isolated boundary tests above.
