# Installed 0.9.0 native UI acceptance — in progress

Observed on 2026-09-11 after the corrected installer was installed. This report separates actual native application behavior from the earlier copied-profile and source-renderer tests.

## Startup and preservation

The actual installed executable launched normally through the supported native app interface at 20:16:14 UTC. Main PID2544, backend PID49616 and renderer PID57424 were returned; API health reports version0.9.0 and backend endpoint127.0.0.1:52439 behind the persistent7332 UI origin. The native window is267938. No profile reset occurred.

The original saved electron-wave project opened directly, with no application exception or error-boundary fallback. Its251 recorded frames rendered as the retained Bloch-state scene. Focusing the actual slider and pressing End visibly reached5/5, with the final trajectory displayed. All47 pre-installation job IDs and states matched exactly:33 completed,9 failed,4 paused,1 cancelled. The new renderer log initially contains only its attachment record.

## Model, timer, draft and retained files

In the existing CAR-T project, the selected model remained OpenAI `gpt-6-astra` with `max` reasoning. The installed model dialog showed six account-supported choices and the requested blue-to-purple gradient slider. Settings displayed provider/account controls and explicitly directed model selection to each conversation; it contained no global model selector.

The native work-time dropdown exposes5minutes,15minutes,1hour,4hours,8hours,Timer off andCustom. A15-minute value was entered through the custom field. The native automation helper's direct value setter returned a UI Automation cache-property error before changing it; ordinary focus/select/type succeeded. This was a helper limitation, not an application exception. Navigating to Settings and back preserved the full unsent revision draft, model/reasoning and900-second value; the matching15-minute preset is displayed after remount.

The previous generated annotation job `93c5db8e-a6c7-4771-badf-7becc237981e` is selectable in Scientific lab. Its actual retained2048×1152PNG displayed with the original aspect ratio, zoom/fit controls, PNG/SVG/caption/manifest download links and expandable retained files/provenance. This verifies output access; that older marketing-style plate does not fulfill the user's newer request for a realistic scientific3D subject.

## Ordinary-chat revision and navigation during work

The approved scientific3D CAR-T revision was sent by clicking the real composer Send button at20:22:47 UTC. Session `158cd192-ea3b-4a42-83a1-eeb118d7ebde` records `open_ai/gpt-6-astra/max` and deadline20:37:47UTC,900seconds from admission. The full entered prompt and admission receipt are preserved in `.local/viewer-acceptance/fifth-native/`.

The actual sidebar shows work spinners and child jobs; the composer exposes a circular Stop control at its right edge. Settings navigation, project-list scrolling and opening the unrelated Diffusion laboratory left the CAR-T session active with the same deadline. The official7URV PDB was downloaded, the intact complex was imported and an independent specialist was delegated. An optional metadata fetch fromdata.rcsb.org was rejected by the existing import-host policy; the officialfiles.rcsb.org PDB fetch succeeded. Final render quality and source-coordinate fidelity remain pending at this report stage.

The completed diffusion control `ca7a01a2-e971-4d14-856f-f7245ecbdb1d` displayed its64×64 field and65 retained states while the CAR-T session continued. The field now fits above a separate readable fixed0.8–1.2 color legend and controls. End reached2.56/2.56seconds after loading the final field; the color range remained fixed. This is a native viewer check, not a rerun or a new scientific validation.

## Actual deadline and bounded continuation

The first CAR-T session stopped as `timed_out` at20:37:48.472UTC against its20:37:47.880UTC deadline. Events114–115 retain the stop and provider-cancellation request. Its automatic conversation compaction ran from20:29:44.278 to20:33:38.046UTC (approximately234seconds of the900-second allocation); the agent resumed afterward. A completed provider usage row with stale retry metadata was investigated separately; queue progression resumed without a restart, so this record does not establish a permanently leaked slot.

No new scientific illustration had rendered at that deadline. The authored scene from generated job `aaabda4a-b1a5-486a-9f6a-3719d7d9e608` was4,934,699bytes and its actual render request was rejected by the4MiB source-file limit. Recovery job `63444fae-056e-4eb4-b445-b814d49fd4dc` completed before timeout with a3,937,166-byte compact scene, but still reported81,415 display vertices, above the existing24,000 aggregate authored-vertex limit. Neither completed authoring job is counted as a successful render. Both inputs, their hashes, the rejection and deadline receipt remain retained.

Diffusion remained selected through timeout: no completion stole focus. A blue unread cue appeared beside CAR-T, and the completed authoring child appeared in the drawer. A new ordinary native follow-up was then sent in the same CAR-T project at20:42:52.764UTC. New session `b89d34bd-0289-4acf-8882-c0984b2781a8` has its own900-second deadline at20:57:52.764UTC, with the original `gpt-6-astra/max` selection. It names the exact retained scene and trusted whole-complex PDB binding and all unchanged authoring limits:4MiB raw source,2MiB normalized compact serialization,24,000 custom vertices and12,000 custom points. The expired session was not silently extended or repeatedly resumed.

## Evidence and remaining checks

Native accessibility receipts are `.local/viewer-acceptance/fifth-native/01-startup-endpoint.txt` through `12-diffusion-final.txt`; actual native screenshots accompany the observation tool records. `.local/viewer-acceptance/fifth-installed-initial-jobs.json` preserves the47-job post-installation snapshot. The UI Automation helper sometimes returned a previous accessibility tree immediately after a route transition; consequential controls were reobserved and actual screenshots/settled state verified before proceeding. No paid request was entered into an unintended project.

Still pending under the user's narrowed scope: actual CAR-T rendered scene/provenance inspection, editable scene access and one bounded presentation-reuse check if feasible. Additional ML studies and broad acceptance expansion are deferred. The working installed backend must remain intact during these jobs. No native graphics benchmark or whole-platform scientific generalization is claimed by this UI report.
